# Eureka run — 2026-04-28 18:15 start

*Continuous discovery loop, ≤60 min wall-clock, fresh $10 budget. Single instruction: DISCOVER something genius. Each entry below is one iteration's landed discovery, signed with a Formula derivation, with the executing commit hash and run-cumulative cost.*

---

## Iteration 1 — 18:18 (~3 min into run)

**Discovery:** Dialogue fence integrity is a **named three-layer operator chain** the repo already implemented but never articulated together — plus a **measurement trap** (clean UI text under-determines prompt success).

**Body (1–3 sentences):** Reading `STYLE_DIALOGUE_INVARIANT`, `orchestrator::strip_asterisk_wrapped_quotes`, and `formatMessage`’s `stripAsteriskWrappedQuotes` + `splitSpokenLinesOutOfEm` as one system surfaces why Ryan can still see “bad formatting” while experiments think the prompt “worked”: Layers B/C silently repair some slips. Honest attribution of Layer A requires **raw completion** (and `finish_reason` when `length` fires `balance_trailing_openers` — synthetic closers are yet another non-semantic fence repair). This updates how the project should read OBSERVATIONS and worldcli bite-tests against fence rules.

**Executing commit:** `7c231cc` — eureka(iteration 1): name dialogue fence stack and attribution trap

**Formula derivation:** 𝓕_fence_chain := Layer_A(prompt_shape) ∘ Layer_B(strip_bare_wrapped_speech) ∘ Layer_C(display_unwind) | trunc_cap ⇒ balance_synthetic

**Gloss:** Readable dialogue is composed repairs; only the prompt layer is the model obeying the invariant.

**Cost this iteration:** $0.00    **Run-cumulative:** $0.00

---

## Closing — 18:20 (~5 min total)

**Iterations completed:** 1

**Total cost:** $0.00 of $10 fresh budget

**Genius bar consistency:** Hit once on cross-boundary read (prompt + orchestrator + frontend); well did not demand a second paid consult this session.

**Forward seed:** Add a small `worldcli` or logging hook that optionally prints **raw vs post-strip** dialogue completion on a flag so fence A/B experiments self-attribute without reading orchestrator source each time.

The run closes here.
